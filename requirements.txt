# =====================================================
# SmartCity Traffic Management System - Requirements
# Render.com Deployment
# =====================================================

# Core Web Framework
flask==3.0.3
flask-cors==4.0.0

# Computer Vision (headless version for server - no GUI needed)
opencv-python-headless==4.9.0.80

# AI / Object Detection
ultralytics==8.1.47

# HTTP requests (kept for optional FastAPI bridge)
requests==2.31.0

# WSGI Server for Production (Render uses this)
gunicorn==22.0.0

# Database (SQLite built-in, but keep for compatibility)
# sqlite3 is part of Python stdlib - no install needed

# Numpy (required by OpenCV + YOLO)
numpy==1.26.4
